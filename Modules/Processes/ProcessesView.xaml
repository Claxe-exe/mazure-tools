<UserControl x:Class="MazureTools.Modules.Processes.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,28,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{DynamicResource Proc_Title}" Style="{StaticResource PageTitle}" />
            <TextBlock Text="{DynamicResource Proc_Subtitle}" Style="{StaticResource Muted}" Margin="0,4,0,18" />
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="{DynamicResource Proc_Restart}" Command="{Binding RestartProcessCommand}" />
                <Button Content="{DynamicResource Proc_End}" Command="{Binding EndProcessCommand}" Style="{StaticResource DangerButton}" Margin="8,0,0,0" />
            </StackPanel>
            <Grid Width="320" HorizontalAlignment="Left">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource Proc_Filter}" />
                <TextBlock Text="{DynamicResource Proc_Filter}" IsHitTestVisible="False" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource Muted}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </DockPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding View}" SelectedItem="{Binding SelectedProcess, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Col_Name}" Binding="{Binding Name}" SortMemberPath="{DynamicResource Col_Name}" Width="2*" />
                    <DataGridTextColumn Header="{DynamicResource Col_Pid}" Binding="{Binding Pid}" SortMemberPath="Pid" Width="90" />
                    <DataGridTextColumn Header="{DynamicResource Col_Cpu}" Binding="{Binding CpuText}" SortMemberPath="CpuPercent" Width="100" />
                    <DataGridTextColumn Header="{DynamicResource Col_Memory}" Binding="{Binding MemoryText}" SortMemberPath="MemoryBytes" Width="120" />
                    <DataGridTextColumn Header="" Binding="{Binding RiskText}" SortMemberPath="Risk" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource Caption}">
                                <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding Status}" Style="{StaticResource Caption}" Margin="2,10,0,0" />
    </Grid>
</UserControl>
