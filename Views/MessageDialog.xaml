<Window x:Class="MazureTools.Views.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource {x:Type Window}}"
        Width="500" SizeToContent="Height" MinHeight="150"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="26,24,26,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="IconText" FontFamily="{StaticResource IconFont}" FontSize="28"
                       VerticalAlignment="Top" Margin="0,2,18,0" />
            <TextBlock x:Name="MessageText" Grid.Column="1" TextWrapping="Wrap" LineHeight="20" />
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button x:Name="ConfirmButton" MinWidth="96" Click="OnConfirm" />
            <Button x:Name="CancelButton" MinWidth="96" Margin="10,0,0,0" IsCancel="True" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>
